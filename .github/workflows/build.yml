# ⚠️ BUILD AUTOMÁTICO DESABILITADO
# 
# Este repositório não contém os arquivos de build necessários (CMakeLists.txt)
# para compilar automaticamente no GitHub Actions.
#
# SOLUÇÃO: Compilar manualmente no Windows
# 1. git pull origin develop
# 2. Compilar usando seu método usual
# 3. O executável terá as correções do efeito 173 em 64x64
#
# Arquivos modificados já commitados:
# - src/client/thingtype.cpp (escala 2x para efeito 173)
# - src/client/effect.cpp (simplificado)

name: Build OTClient

# DESABILITADO - Sem arquivos de build
# on:
#   workflow_dispatch:
#   push:
#     branches: [ develop, main ]

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Build Disabled
        run: |
          echo "⚠️ Build automático desabilitado"
          echo "💡 Este repositório não contém CMakeLists.txt"
          echo "📋 Compile manualmente no Windows"
          echo "✅ Arquivos fonte modificados já estão commitados"
