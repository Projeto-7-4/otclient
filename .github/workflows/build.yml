# ⚠️ BUILD AUTOMÁTICO DESABILITADO
# 
# O repositório otclient não contém os arquivos de build necessários
# (CMakeLists.txt, estrutura completa do código fonte)
# 
# Para compilar:
# 1. Use o repositório otclient-build que tem o código fonte completo
# 2. Ou compile manualmente no Windows usando seu método usual
# 3. Os arquivos fonte modificados (thingtype.cpp, effect.cpp) já estão commitados

name: Build OTClient

on:
  workflow_dispatch:  # Apenas manual, mas não funcionará sem CMakeLists.txt

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Error - Build files missing
        run: |
          echo "❌ Este repositório não contém CMakeLists.txt"
          echo "💡 Para compilar, use o repositório com código fonte completo"
          echo "   ou compile manualmente no Windows"
          exit 1
